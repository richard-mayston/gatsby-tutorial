import * as React from "react"
import Layout from "../components/layout";

const AboutPage = () => {
    return (
        <Layout pageHeading="About Us">
            <p>About us intro</p>
        </Layout>
    )
};

export const Head = () => <title>About</title>

export default AboutPage;