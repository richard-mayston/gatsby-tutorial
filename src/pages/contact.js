import * as React from "react"
import Layout from "../components/layout";

const ContactPage = () => {
    return (
        <Layout pageHeading="Contact Us">
            <p>Contact us intro</p>
        </Layout>
    )
};

export const Head = () => <title>Contact</title>

export default ContactPage;